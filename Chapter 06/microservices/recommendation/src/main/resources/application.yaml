spring:
  application:
    name: RecommendationService

spring.data.mongodb:
  host: localhost
  port: 27017
  database: product-db
  auto-index-creation: true

logging:
  level:
    root: INFO
    com.iocodes.app: DEBUG

server.port: 7002

---
spring.config.activate.on-profile: docker
server.port: 8080