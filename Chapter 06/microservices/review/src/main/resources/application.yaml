spring:
  application:
    name: ReviewService

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.iocodes.app: DEBUG

server.port: 7003


---
spring.config.activate.on-profile: docker
server.port: 8080